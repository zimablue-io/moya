# Active context

## Current focus

Model default is on-device GGUF. Hosts without an engine remap to Custom, never xAI. Voice default is Custom (empty URL). Speakers list from the configured endpoint. Desktop also offers Local voice. Provider names share `PROVIDER_PRESETS`. First open is a three-step onboarding dialog (provider, voice, soul). Settings stays closed until those three are written. Greetings do not send the tool catalog. GGUF weights still unload on hide-to-tray, Quit, leaving on-device, and ~5 min idle.
