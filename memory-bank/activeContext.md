# Active context

## Current focus

Setup was a second Model form. Voice and Model are two Settings tabs on purpose (speak vs think). The first-run sheet now renders `ModelTab` — same control as Settings → Model.

## Next

Reload. Talk opens that sheet; picking a GGUF writes Settings → Model. Hear Talk, or say you have not.
